---
layout: page
title: Thoughts
permalink: /thoughts/
---

{% find posts where site.posts, title != "" %}

<ul>
  {% for post in posts %}
    <li>
      <a href="{{post.url}}">{{post.title}}</a>
    </li>
  {% endfor %}
</ul>
