---
# Feel free to add content and custom Front Matter to this file.

layout: home
---

{% for post in site.posts %}
  {% if post.title != '' %}
    {% render "article", post: post %}
  {% else %}
    {% render "thought", thought: post %}
  {% endif %}
{% endfor %}
