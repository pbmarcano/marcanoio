---
layout: page
title: "Projects"
---
{% for project in site.projects %}
  <article>
    <a href="{{ project.url }}">
      <h2>{{ project.title }}</h2>
    </a>
    {{ project.excerpt | markdownify }}
  </article>
{% endfor %}
